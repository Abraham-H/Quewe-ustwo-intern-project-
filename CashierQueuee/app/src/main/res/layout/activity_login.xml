<views.LoginActivityLinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:id="@+id/add_to_queue_linear_layout"
    app:header_text="@string/welcome_text"
    app:subheader_text="@string/help_text"
    app:footer_text="@string/scanning_text"/>
